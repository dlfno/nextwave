services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-nextwave}
      POSTGRES_USER: ${POSTGRES_USER:-nextwave}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-nextwave_dev}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 2s
      timeout: 3s
      retries: 15
    volumes:
      - nextwave_postgres_data:/var/lib/postgresql/data

volumes:
  nextwave_postgres_data:
